# ==== System & IDEs ====
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# ==== Virtual Environments & Caches ====
.venv/
venv/
env/
.pdf_venv/
# WS1.2: deploy_detections.sh bootstraps the Sigma toolchain here.
.venv-detections/
.venv-sigma/
__pycache__/
*.pyc
*.pyo
*.pyd

# ==== Raw Network & Log Data ====
*.pcap
*.pcapng
*.log
*.csv
zeek_logs/
filebeat_data/

# Zeek runtime artifacts (generated at capture time, never commit)
conn.log
http.log
dns.log
ntp.log
files.log
weird.log
reporter.log
packet_filter.log
loaded_scripts.log
stderr.log
stdout.log
.state/

# Docker named volumes
suburban_soc_data/

# ==== Secrets & Credentials ====
.env
.env.*
# ...but DO track the template (no real secrets in it)
!.env.example
*.pem
*.key
*.cert
*.crt
id_rsa*
*.ovpn
secrets/
credentials.json
# Broker router inventory — keep the template, not the real one.
scripts/hive-mind-broker/inventory.yaml

# ==== Important / Raw Documents ====
# Prevent raw working documents and reference materials from being exposed
*.pdf
*.docx
*.doc
*.xlsx
pdf_content.txt

# ==== Build / Temp Dirs ====
/tmp/
/temp/
node_modules/
.antigravitycli/

# ==== Secrets / TLS material / SOAR runtime state (CDP §6, §12.3) ====
.env
scripts/setup/.env
scripts/setup/certs/
**/approval_queue.jsonl

# Python virtual environments
.venv/
.pdf_venv/
tests/anomaly_simulation/.venv/

# Python runtime caches
__pycache__/
*.pyc
.pytest_cache/
scripts/hive-mind-broker/.pytest_cache/
.coverage

# Local deployment configurations
scripts/setup/.env
scripts/hive-mind-broker/inventory.yaml
.antigravitycli/

# Local audit scratch / generated dumps — not deliverables, never commit.
# (coverage_checklist.md IS a deliverable and stays tracked.)
/AUDIT_REPORT.md
/repo_audit_packet.md
/generate_audit.py
